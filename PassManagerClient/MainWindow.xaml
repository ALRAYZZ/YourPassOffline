<Window x:Class="PassManagerClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PassManagerClient"
        mc:Ignorable="d"
        Title="YourPassOffline" Height="450" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Enter Decryption Key:" VerticalAlignment="Center" Margin="0,0,10,0" />
            <TextBox Width="200" Text="{Binding Key, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Open Vault" Margin="0,0,10,0" Command="{Binding OpenVaultCommand}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <TextBlock Text="Site:" Width="50"/>
                <TextBox Width="200" Text="{Binding NewSite, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <TextBlock Text="Username:" Width="50"/>
                <TextBox Width="200" Text="{Binding NewUsername, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <TextBlock Text="Password:" Width="50"/>
                <TextBox Width="200" Text="{Binding NewPassword, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
        </StackPanel>


        <ListBox Grid.Row="2" ItemsSource="{Binding VaultEntries}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Site}" Width="100"/>
                        <TextBlock Text="{Binding Username}" Width="100"/>
                        <TextBlock Text="{Binding Password}" Width="100"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
